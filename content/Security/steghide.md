---
tags:
  - forensics
  - security
---
A tool used to hide data within other data formats and can be used to extract other [[Polyglot]] files.
# Embedding a File
```
steghide embed --coverfile path/to/image.png --embedfile path/to/data.txt
```