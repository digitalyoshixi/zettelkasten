---
tags:
  - binary_exploitation
  - security
---
When the value of an integer exceeds its maximum allowable size.

- If its a signed integer, then it wraps around to negative number.
- If its an unsigned integer, it wraps around to 0