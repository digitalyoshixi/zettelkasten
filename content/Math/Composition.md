---
tags:
  - math
---
written like `f(g(x))` or `(f∘g)(x)` the `∘` between f and g is the composition symbol
function inside another function
`g(x)` is the inner function
`f(x)` is the outer function
# Input Process for Evaluation
in the function `f(g(x))`
![[Composite Functions-20231219004837285.webp]]

# How to find Domain
1. Identify domain restrictions of the inner function
2. Find composite function
3. Identify domain restrictions of composite function
4. Find domain includes both restrictions
DOMAIN FOR $f o g \neq g o f$ 
# Function and its inverse
Graphing $f^{-1}(f(x))$
will turn out to be y=x, WITH RESTRICTIONS. restrictions depend on the 2 functions

$(fog)^-1=(g^{-1}of^{-1})$

