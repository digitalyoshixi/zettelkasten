---
tags:
  - calculus
  - math
aliases:
  - DNE
---
The formal definition for does not exist is:
$\exists \epsilon > 0$ s.t $\forall \delta \in R$, $\exists x \in R$ s.t $0<|x-c| < \delta$ and $|h(x)-L| \geq \epsilon$
Alternatively, you can intuit it as:
$$\forall L\in R, \lim_{ x \to c }h(x) \neq L $$
# Proof Process
1. Choose $\epsilon = ???$ (Note $\epsilon>0$)
2. Let $\delta > 0$ be arbitrary
3. Let $x_{1},x_{2} \in I$ where 
# Occurrences
- Occurs at oscillating values, infinities and x/0.