---
tags:
  - networking
aliases:
  - CDMA
---
A implementation of [[Multiplexer|Multiplexing]] that converts several streams of call information into one stream, so that each call can share the same frequency.
Each call is given a code, that allows clients to find their call in the multiplexed stream.
CDMA phones have no [[Subscriber Identity Module|SIM]] card