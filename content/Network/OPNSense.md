---
tags:
  - networking
---
A open-source software [[Router]] that runs on [[FreeBSD]]
# Default Credentials
```
root : opnsense
installer : opnsense
```
# Firewall Setting
```bash
pfctl -d # disable firewall
pfctl -e # enable firewall
```