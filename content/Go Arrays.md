---
tags:
  - go
  - programming
---
```go
// declare the length initialy
var array_name = [length]datatype{values}

// let the length be automatic
var array_name = [...]datatype{values}

// declaring with walrus
array_name := [...]_datatype_{_values_}
```
