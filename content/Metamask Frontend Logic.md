---
tags:
  - blockchain
---
# Process
1. Everytime you connect your [[Metamask]] wallet to the browser, an `ethereum` variable is injected into your browser window. This allows us to tell if a browser is web3 compatible
2. Check if `ethereum` is null
3. If `ethereum` is not null, get the `chainID`
# Code
