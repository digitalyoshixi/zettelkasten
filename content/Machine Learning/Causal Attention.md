---
tags:
  - machine_learning
---
The idea that a model can only move forward by following its previously generated outputs.
It never moves forward depending on the training data itself, but rather only the last outputs, like a [[Markov Chain]].
