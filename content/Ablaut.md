---
tags:
  - lingustics
---
A method for [[Internal Change]] that is cause when a single [[Vowel]] within a [[Word]] is changed.
# Examples
- `sing -> sang`. [[Ablaut]] is `i -> a`
- `sink -> sank`. [[Ablaut]] is `i -> a`