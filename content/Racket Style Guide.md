---
tags:
  - programming
  - lisp
---
- Identifiers should use `-` over `_`
- Arguments placed on same line, or same column
- Multiple close parentheses placed one same line
- You should use `[]` instead of `()` if you can like in [[Racket cond]]