---
tags:
  - windows
aliases:
  - stop-computer
---
# Reboot
Do a clean shutdown before reboot
`shutdown /r /t 0`
# Instant Shutdown Command
`shutdown /s /t 0`
# Shutdown Remote Server
`shutdown /s /m \\devserver`
# Shutdown Abort
`shutdown /a`