---
tags:
  - web
  - networking
---
A function that sets an event listener for a specific event.
# Boilerplate
```js
// recieve an event (myeventresponse)
socket.on('myeventresponse', (number) => {
    console.log(number);
})
```