---
tags:
  - linux
---
A [[Unix]] directory that contains subdirectories for every process running on the system, denoted by their [[Process ID]].
# Viewing the program memory map
```
cat /proc/<num>/map
```
