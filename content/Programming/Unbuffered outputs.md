---
tags:
  - programming
---
the opposite of [[Buffered outputs]]. each request is sent directly to the output device.

buffering is faster than unbuffered outputs, because writing to a memory location can be done very quickly as opposed to the slow transfers that occur due to multiple output requests