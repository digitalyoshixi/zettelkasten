---
tags:
  - cpp
---
whitespace
its to remove leading whitespace from the input stream.
That means that whenever we type something like space, tab or newline before or after our input, that gets sanitized at the beginning of the next input
paired with [[cin|std::cin]] inside of [[getline()|std::getline]]
