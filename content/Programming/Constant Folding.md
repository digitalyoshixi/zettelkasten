---
tags:
  - compilers
---
Evaluating constant expressions at compile time rather than at run-time.
- If [[Arithmetic]] operations can be performed (`1+3` -> `4`)
- If [[Boolean]] operations can be performed (`b & false` -> `false`)
# Implementations
- [[C++ Constants]]