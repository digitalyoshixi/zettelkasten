---
tags:
  - c
---
`strlen(str)`
returns the length of a given string.

```c
char str1[] = "hihihie";
    int strleng = strlen(str1);

    printf("%d\n", strleng);
```
# Parameters
A single string value. either variable or direct value.
# Returns
integer of the length of a string